#include "main.h"


/**
 * _abs - function that computes
 * @h: character checked
 * Description: in lowercase or uppercase
 * Return: absolute value of an integer
 */

int _abs(int h)
{
	if (h >= 0)
		return (h);
	else
		return (-h);
}
